def create_a_student(first_name, last_name, age)

class Native:
    def __init__(self, first_name, last_name, email, phone_number, sex, next_of_kin):
        self.first_name = first_name
        self.last_name = last_name
        self.email = email
        self.phone_number = phone_number
        self.sex = sex
        self.courses = []
        self.next_of_kin = next_of_kin
    def _str_(self) -> str:
        return self.first_name + " " + self.last_name


class NexTOfKin:
    def _init_(self, first_name, last_name, phone_number, sex, relationship):
        self.first_name = first_name
        self.last_name = last_name
        self.

print(first_example.cohort_eight)